function ShowReport19A() {
    var posleft = (screen.width - 700) / 2;
    var postop = (screen.height - 500) / 2;
    var page = "partials/report_19a.aspx"
    var pageTitle = "Report19A";
    var width = 735;
    var height = 500;

    ShowPage(posleft, postop, page, pageTitle, width, height);
} //ShowReport19A


function ShowReport19B() {
    var posleft = (screen.width - 750) / 2;
    var postop = (screen.height - 500) / 2;
    var page = "partials/report_19b.aspx"
    var pageTitle = "Report19B";
    var width = 950;
    var height = 500;

    ShowPage(posleft, postop, page, pageTitle, width, height);
} //ShowReport19B

function ShowReport19C() {
    var posleft = (screen.width - 750) / 2;
    var postop = (screen.height - 500) / 2;
    var page = "partials/report_19c.aspx"
    var pageTitle = "Report19C";
    var width = 950;
    var height = 500;

    ShowPage(posleft, postop, page, pageTitle, width, height);
} //ShowReport19C